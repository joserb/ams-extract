# Protocolo de verificación visual contra AMS

> Cómo validar que lo extraído por `ams-extract` coincide con lo que un
> analista vería en AMS Machinery Manager para el mismo punto y timestamp.

Estado: **plantilla vacía** — se completará en Fase 4 (ver `docs/PLAN.md` §5).

## 1. Objetivo

Confirmar, con humano en el bucle, que la forma, magnitud y ejes de los
espectros FFT extraídos coinciden con los que muestra AMS.

## 2. Material necesario

- VM Windows XP con AMS Machinery Manager y la base `.rbm` original cargada.
- Ejecutar `rbm extract` contra el mismo fichero, mismo punto, mismo timestamp.
- Capturar screenshot de AMS y PNG generado por `ams-extract`.

## 3. Pasos

Pendiente.

## 4. Criterios de aceptación

Pendiente — borrador: al menos 2 espectros de 2 puntos distintos coinciden
visualmente dentro de tolerancia razonable.

## 5. Registro de verificaciones realizadas

| Fecha | Punto | Timestamp | Resultado | Notas |
|---|---|---|---|---|
| _pendiente_ | | | | |
